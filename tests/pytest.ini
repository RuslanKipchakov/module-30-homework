[pytest]

testpaths = tests

markers =
    asyncio: marks tests as asyncio-based, to be run in an event loop

addopts = --asyncio-mode=auto

asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

filterwarnings =
    ignore::Warning
